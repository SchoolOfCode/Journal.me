import React from "react";
import "./style.css"

function ArrowButton({text}) {
    return (
        <>
        <button className="arrow-btn">{text}</button>
        </>
    )
}

export default ArrowButton;